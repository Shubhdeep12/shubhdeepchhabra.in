import { FC } from "../../types/FC";

type IconType = {
    width: 10,
    height: 10,
    color: '#000000',
    type: 'light',
}

export const ThemeToggle: FC<IconType> = ({ color, width, height, type }) => {
    return ( <>hh</>)
}